/**
 * @fileoverview
 * ship.ts includes all the classes and interfaces to describe the various ships
 * the player and planets can have in their fleets.
 */

/** Vessle enum represents the various types of ships that exist */
export enum Vessle {
  FIGHTER = 'fighter',
  TRANSPORT = 'transport',
  M_TRANSPORT = 'manned-transport',
  SUPPLY = 'supply',
  FUEL = 'fuel',
}
